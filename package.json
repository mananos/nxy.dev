{
  "name": "nxy",
  "version": "1.0.0-rc.1",
  "private": true,
  "description": "Claude Code plugin: fewer tokens without losing quality — metrics, output filtering, scout, write gate, plan, verification, review and memory",
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "test": "node --disable-warning=ExperimentalWarning --test \"tests/**/*.test.mjs\"",
    "typecheck": "tsc -p jsconfig.json",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
