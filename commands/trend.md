---
description: Consumption trend over time — by day, week or session, all projects (nxy)
argument-hint: [--since 30d] [--by day|week|session|model] [--here] [--json]
allowed-tools: Bash(node:*)
---

Below is a report generated by a script. Your entire reply must be that report inside one fenced code block, reproduced **character for character**: every line, every column, every `0`, every `?`, the legend once. Do not align, reformat, summarize, or comment. A dropped cell is a wrong answer. Only after the block, and only if the user asked a question, answer it from these numbers.

```
!`node "${CLAUDE_PLUGIN_ROOT}/hosts/claude-code/entries/trend.mjs" $ARGUMENTS`
```
