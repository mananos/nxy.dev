---
description: Write gate — stops the main thread from editing once its context is expensive (nxy)
argument-hint: [status | on | off | once]
allowed-tools: Bash(node:*)
---

Below is a report generated by a script. Your entire reply must be that report inside one fenced code block, reproduced character for character. Do not reformat, summarize, or comment. Only after the block, and only if the user asked a question, answer it from these numbers.

```
!`node "${CLAUDE_PLUGIN_ROOT}/hosts/claude-code/entries/gate.mjs" $ARGUMENTS`
```
