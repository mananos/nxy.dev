---
description: Start a code change the nxy way — recipe by size, a plan when it needs one (nxy)
argument-hint: <what to build or change>
---

Use the `nxy-workflow` skill for this task: $ARGUMENTS

First decide its size with the recipe and say it in one line (small / medium / large, and why). Then follow the recipe for that size.
